{
  "config": {
    "step": {
      "user": {
        "title": "Lost Entity Finder",
        "description": "Detect lost entity references after entity ID changes and raise repairs with direct links to each location.",
        "data": {
          "enable_bulk_fix": "Enable Auto-Replace (bulk fix)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Lost Entity Finder is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Lost Entity Finder options",
        "data": {
          "enable_bulk_fix": "Enable Auto-Replace (bulk fix)"
        }
      }
    }
  },
  "issues": {
    "lost_entity_references": {
      "title": "Lost entity references to {old_entity_id}",
      "fix_flow": {
        "step": {
          "choose_action": {
            "title": "Fix lost entity references to {old_entity_id}",
            "description": "Choose how to handle references to `{old_entity_id}` (changed to `{new_entity_id}`).\n\n{references}\n\n{manual_note}{bulk_fix_hint}",
            "data": {
              "action": "Action"
            }
          },
          "auto_replace_disabled": {
            "title": "Auto-Replace disabled",
            "description": "Auto-Replace is disabled in Lost Entity Finder integration settings. Enable bulk fix under **Settings → Devices & services → Lost Entity Finder → Configure**, or use the edit links above to update references manually."
          },
          "manual_only": {
            "title": "Manual update required for {old_entity_id}",
            "description": "References to `{old_entity_id}` (changed to `{new_entity_id}`) must be updated manually:\n\n{references}\n\n{manual_note}Use the links above to edit each location, then submit to close this repair."
          },
          "preview": {
            "title": "Preview Auto-Replace",
            "description": "The following locations will be updated from `{old_entity_id}` to `{new_entity_id}`:\n\n{preview}\n\nConfirm to apply these changes."
          },
          "result": {
            "title": "Auto-Replace complete",
            "description": "{result_summary}"
          }
        }
      }
    }
  }
}
